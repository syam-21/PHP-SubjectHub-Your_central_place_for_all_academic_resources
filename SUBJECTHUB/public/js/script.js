// SUBJECTHUB - Custom JavaScript
// ---------------------------------

document.addEventListener('DOMContentLoaded', function() {
    console.log('SubjectHub Loaded!');

    // Add any interactive elements here later
    // Example: const buttons = document.querySelectorAll('.interactive-button');
    // buttons.forEach(button => {
    //     button.addEventListener('click', () => {
    //         // do something cool
    //     });
    // });
});
